{
  "name": "vlauncher-desktop",
  "private": true,
  "scripts": {
    "build:app": "npm --prefix vite-src run check && node vite-src/node_modules/@neutralinojs/neu/bin/neu.js build",
    "check": "npm --prefix vite-src run check",
    "test": "npm --prefix vite-src test"
  }
}
